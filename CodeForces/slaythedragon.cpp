#include<bits/stdc++.h>
using namespace std;
int main(){
    int n, sum=0;
    cin>>n;
    int a[n];
    for(int i=0;i<n;i++){
        sum+=a[i];
        cin>>a[i];
    }
    int m;
    cin>>m;
    int b[m][2];
    for(int i=0;i<m;i++){
        cin>>b[i][0]>>b[i][1];
    }
    sort(a, a+n);
    for(int i=0;i<m;i++){
        if(m[   ])
    }
    return 0;
}