#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;
int main(){
    ll t;
    cin>>t;
    while(t--){
        ll n, aminindex, bmaxindex, aminflag=0, bmaxflag=0;
        cin>>n;
        ll a[n], b[n];
        for(ll i=0;i<n;i++){
            cin>>a[i];
        }
        for(ll i=0;i<n;i++){
            cin>>b[i];
        }
        if(n>1){
            if()
        }
    }
    return 0;
}